# All-keys template — used by tests/test_render_prompt.bats

REPO_OWNER=${REPO_OWNER}
REPO_NAME=${REPO_NAME}
REPO_SLUG=${REPO_SLUG}
DEFAULT_BRANCH=${DEFAULT_BRANCH}
BRANCH_PREFIX=${BRANCH_PREFIX}
WORKTREE_BASE=${WORKTREE_BASE}
LOCK_DIR=${LOCK_DIR}
DISPATCH_LOCK_DIR=${DISPATCH_LOCK_DIR}
SEVERITY_LABEL_HIGH=${SEVERITY_LABEL_HIGH}
SEVERITY_LABEL_MEDIUM=${SEVERITY_LABEL_MEDIUM}
SEVERITY_LABEL_LOW=${SEVERITY_LABEL_LOW}
TYPE_LABELS=${TYPE_LABELS}
TRIAGE_CORE_FILES_REGEX=${TRIAGE_CORE_FILES_REGEX}
TRIAGE_TESTS_REGEX=${TRIAGE_TESTS_REGEX}
TRIAGE_CI_SECRETS_REGEX=${TRIAGE_CI_SECRETS_REGEX}
TRIAGE_LINE_LIMIT=${TRIAGE_LINE_LIMIT}
DEV_INSTANCES_DEFAULT=${DEV_INSTANCES_DEFAULT}
POLL_INTERVAL_DEFAULT=${POLL_INTERVAL_DEFAULT}
EMPTY_CYCLE_BACKOFF_CAP_SECONDS=${EMPTY_CYCLE_BACKOFF_CAP_SECONDS}
DEV_MAX_TURNS=${DEV_MAX_TURNS}
REVIEWER_MAX_TURNS=${REVIEWER_MAX_TURNS}
DEV_CI_RETRY_ATTEMPTS=${DEV_CI_RETRY_ATTEMPTS}
DEV_FOLLOWUP_CYCLE_LIMIT=${DEV_FOLLOWUP_CYCLE_LIMIT}
REVIEWER_BASH_CALL_BUDGET=${REVIEWER_BASH_CALL_BUDGET}
STALE_LOCK_HOURS=${STALE_LOCK_HOURS}
TEST_CMD=${TEST_CMD}
DEV_AGENT_COMMENT_PREFIX=${DEV_AGENT_COMMENT_PREFIX}
DEV_AGENT_PR_BODY_TAG=${DEV_AGENT_PR_BODY_TAG}
REVIEWER_AGENT_COMMENT_PREFIX=${REVIEWER_AGENT_COMMENT_PREFIX}
REVIEWER_AGENT_VERDICT_REGEX=${REVIEWER_AGENT_VERDICT_REGEX}

# Variables OUTSIDE the RIG_KEYS allowlist must pass through literally
# so the agent expands them via bash at runtime.
PASSTHROUGH_REPO_ROOT=${REPO_ROOT}
PASSTHROUGH_HOLDER=${HOLDER}
