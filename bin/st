#!/usr/bin/env bash
# st — global CLI for sietch (a multi-agent dev loop framework).
#
# Sietch is modeled on Steve Yegge's GasTown, named after the Fremen
# community concept from Dune. Each consumer repo carries a single
# .sietch/rig.config; the framework code lives here in $SIETCH_HOME
# and is shared across every rig you onboard.
#
# Invoke from anywhere inside a repo that has .sietch/rig.config:
#   st dev                        # scan issues, claim one, drive a PR
#   st dev follow-up <PR>         # address reviewer-agent feedback
#   st dev resolve <PR>           # auto-resolve merge conflicts (triage-gated)
#   st review                     # dispatch a per-PR reviewer sub-agent
#   st issue [message]            # interactive issue author
#   st loop [start|stop|...]      # tmux multi-pane orchestrator
#   st triage <PR>                # decide whether a conflict is auto-resolvable
#   st init                       # bootstrap .sietch/rig.config in $PWD
#   st version                    # short SHA of the framework
#   st help                       # this message

set -u

SIETCH_HOME="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export SIETCH_HOME

usage() {
  sed -n '/^# st — /,/^# *st help/p' "$0" | sed -E 's/^# ?//'
}

# Walk up from $PWD until we find a .sietch/rig.config. Echoes the dir,
# returns 1 if not found.
find_repo_root() {
  local d="$PWD"
  while [ "$d" != "/" ] && [ -n "$d" ]; do
    if [ -f "$d/.sietch/rig.config" ]; then
      echo "$d"
      return 0
    fi
    d=$(dirname "$d")
  done
  return 1
}

require_repo_root() {
  REPO_ROOT="$(find_repo_root)" || {
    echo "[st] no .sietch/rig.config found in $PWD or any parent." >&2
    echo "Run 'st init' from your repo root to bootstrap one." >&2
    exit 1
  }
  export REPO_ROOT
}

cmd_init() {
  local dest="$PWD/.sietch"
  local config="$dest/rig.config"
  if [ -f "$config" ]; then
    echo "[st init] $config already exists. Edit it directly." >&2
    return 1
  fi
  mkdir -p "$dest"
  cp "$SIETCH_HOME/templates/rig.config.example" "$config"
  cat > "$dest/README.md" <<EOF
# Sietch — agent-dev-loop config for this rig

This directory carries the per-rig configuration for the [Sietch](https://github.com/aniryou/sietch)
multi-agent dev loop. The framework itself lives in \`\$SIETCH_HOME\` (typically
\`~/code/sietch/\`); this directory holds only \`rig.config\`, which the framework
substitutes into prompt templates at invocation time.

## Edit rig.config

Set \`REPO_OWNER\`, \`REPO_NAME\`, severity labels, branch prefix, and any
project-specific overrides. Defaults are documented inline.

## Run the agents

From anywhere inside this repo:

    st dev          # scan issues, claim one, drive a PR
    st review       # review an open dev-agent PR
    st loop start   # run the full multi-agent fleet in tmux
    st help         # full command list
EOF
  echo "[st init] created $config"
  echo "[st init] edit it to set REPO_OWNER, REPO_NAME, etc."
}

cmd_version() {
  if [ -d "$SIETCH_HOME/.git" ]; then
    git -C "$SIETCH_HOME" describe --always --dirty 2>/dev/null \
      || git -C "$SIETCH_HOME" rev-parse --short HEAD 2>/dev/null \
      || echo "unknown"
  else
    echo "unknown (SIETCH_HOME is not a git repo)"
  fi
}

CMD="${1:-help}"
shift || true

case "$CMD" in
  help|-h|--help)  usage ;;
  version)         cmd_version ;;
  init)            cmd_init "$@" ;;
  dev|developer)   require_repo_root; exec "$SIETCH_HOME/wrappers/run-developer.sh" "$@" ;;
  review|reviewer) require_repo_root; exec "$SIETCH_HOME/wrappers/run-reviewer.sh" "$@" ;;
  issue|issues)    require_repo_root; exec "$SIETCH_HOME/wrappers/run-issue-author.sh" "$@" ;;
  loop)            require_repo_root; exec "$SIETCH_HOME/wrappers/run-loop.sh" "$@" ;;
  triage)          require_repo_root; exec "$SIETCH_HOME/wrappers/triage-conflict.sh" "$@" ;;
  *)
    echo "[st] unknown command: $CMD" >&2
    echo "Run 'st help' for the command list." >&2
    exit 2
    ;;
esac
