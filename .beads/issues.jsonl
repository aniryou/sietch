{"_type":"issue","id":"sietch-06t","title":"dispatcher loops fan out unbounded background dev-agent runs","description":"loop_dispatcher_followup and loop_dispatcher_conflicts in wrappers/run-loop.sh:123-219 background a child dev-agent per eligible PR per cycle via `( ... ) \u0026`. The only governor is the per-PR lock dir (pr-\u003cn\u003e-followup.lock / pr-\u003cn\u003e-conflicts.lock); concurrency is bounded only by lock contention, NOT by a worker cap.\n\nReal-world dev-agent runs take 5-15 minutes; POLL_INTERVAL_DEFAULT is 60s. If 4 PRs are eligible and each agent run takes 10 min, you accumulate ~40 simultaneous backgrounded claude invocations across 10 cycles before the first finishes. Memory, API cost, and gh rate-limit pressure all scale with this without a ceiling.","design":"Add a concurrency cap shared between followup and conflicts dispatchers (or per-dispatcher). Two viable shapes:\n\nA) Count active dispatch locks before backgrounding:\n   - Define DISPATCH_MAX_CONCURRENT in rig.config.example (default 3, mirroring DEV_INSTANCES_DEFAULT)\n   - Before each `( ... ) \u0026`, count `find $DISPATCH_LOCK_DIR -name '*.lock' -type d | wc -l`\n   - If \u003e= cap, skip remaining eligible PRs this cycle; they'll re-enter next cycle\n\nB) Use a counting semaphore via `flock` on a fixed-size pool of lock files\n   - Heavier but more accurate\n\nRecommendation: A. Simpler, matches existing lock-dir mental model, and the per-cycle re-scan handles fairness.","acceptance_criteria":"- DISPATCH_MAX_CONCURRENT (or equivalent) is documented in templates/rig.config.example with a default\n- Both dispatcher loops respect the cap before backgrounding a child\n- Cap counts active (live-PID) dispatch locks; stale-lock cleanup still runs first\n- When at cap, the dispatcher logs \"[dispatch:X] at cap (N/N); skipping remaining\" and sleeps the normal interval\n- No regression in the existing per-PR lock semantics — duplicate dispatch for the same PR still impossible\n- A doc comment in run-loop.sh explains the cap and how it interacts with DEV_INSTANCES (the foreground worker count)","status":"open","priority":2,"issue_type":"bug","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:10:18Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:10:18Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-06r","title":"eligibility_dev_count: skip already-locked issues to avoid wasted LLM scans","description":"When DEV_INSTANCES dev-agents poll concurrently and the eligible-issue queue is small, every worker sees the same gh-issue count, every worker invokes the LLM, and N-1 of them lose the atomic-mkdir lock race inside the agent. Each loss is one wasted LLM scan per cycle.\n\nlib/eligibility.sh:41-56 deliberately ignores LOCK_DIR — the comment (\"false positives just cost a single agent run\") justifies this for a single worker, but at DEV_INSTANCES_DEFAULT=3 the cost is 3x per cycle whenever the queue is short. This is the dominant waste path in steady state.","design":"Post-filter the gh issue list against $LOCK_DIR/gh-\u003cnum\u003e.lock inside eligibility_dev_count before counting. Cheap (one filesystem stat per candidate issue), safe (atomic mkdir remains the source of truth — this is just a hint), and removes the wasted-cycle path without changing the lock semantics.\n\nOptional: also filter against issues that already have an open dev-agent/* PR (gh pr list once per cycle, jq-join). Defer this if simple lock-dir filter is enough in practice.","acceptance_criteria":"- eligibility_dev_count returns 0 when every gh-eligible issue already has a $LOCK_DIR/gh-\u003cnum\u003e.lock dir\n- Lock semantics unchanged — the post-filter is advisory; the agent still does the atomic mkdir\n- Multi-worker steady state: at most one wrapper invokes the LLM per (eligible-issue, cycle) pair\n- Stale-lock behavior unchanged — STALE_LOCK_HOURS cleanup still runs in the agent prompt\n- A test or fixture demonstrates the new filter path (paired with sietch-tests issue)","status":"open","priority":2,"issue_type":"bug","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:10:06Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:10:06Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-5ek","title":"add 'st onboard' to audit a rig for sietch's loop prerequisites","description":"The sietch loop assumes scaffolding the framework doesn't currently verify: pre-commit hooks (so dev-agent commits get auto-formatted before push), GitHub Actions CI (so the agent has a real signal to gate merge on — without it, 'gh pr checks' returns vacuously-green and the agent ships junk), and a test runner with at least one test (so CI's signal is meaningful and the agent's CI-retry loop has something to read from on red).\n\nToday, a user retrofitting sietch onto an existing repo finds out about each missing piece by reading prompt failures or by debugging a silent merge of broken code. 'st onboard' should walk the rig once, report pass/fail per check, and emit copy-pasteable fix snippets for each gap so the user can either fix manually or pipe through bash.","design":"New file: bin/st gains 'onboard' subcommand → calls lib/onboard.sh.\n\nlib/onboard.sh structure:\n  - Each check is a named function returning 0/1 + an OK/FAIL printout\n  - Functions: check_rig_config, check_beads, check_gh_auth, check_pre_commit_config, check_pre_commit_installed, check_ci_workflow, check_ci_pr_trigger, check_test_dir, check_test_files, check_worktree_base\n  - Aggregator runs them all, accumulates pass/fail tally, prints fix snippets for fails\n  - Fix snippets live inline in each check function — kept close to the predicate so they don't drift\n\nOutput format:\n  ✓ rig.config            REPO_SLUG=aniryou/foo\n  ✓ .beads/\n  ✓ gh auth (aniryou)\n  ✗ .pre-commit-config.yaml\n      Fix: cat \u003e .pre-commit-config.yaml \u003c\u003c'EOF'\n           repos:\n             - repo: https://github.com/astral-sh/ruff-pre-commit\n               rev: v0.7.4\n               ...\n           EOF\n           pip install pre-commit \u0026\u0026 pre-commit install\n  ✗ CI workflow\n      Fix: mkdir -p .github/workflows\n           cat \u003e .github/workflows/ci.yml \u003c\u003c'EOF' ...\n  ...\n\n  Result: 7/10 checks passed; 3 missing. Run 'st onboard -v' for details.\n\nOut of scope (file separately):\n  - 'st onboard --fix' to auto-apply fixes (risky; v1 leaves writes to the user)\n  - Language-specific test scaffolding (Python pytest vs Node jest etc.) — onboard reports presence; doesn't generate\n  - Per-rig override of which checks apply (e.g. 'this repo doesn't need GH Actions because it uses CircleCI')","acceptance_criteria":"- 'st onboard' runs from anywhere inside a rig (walks up to find .sietch/rig.config) and prints a pass/fail report\n- Read-only by default — never mutates the rig or runs install commands without explicit '--fix' (out of scope for v1)\n- Exit code 0 if all checks pass, 1 if any fail (suitable for CI-style usage)\n- Each failed check prints a concrete copy-pasteable fix command/snippet\n- Checks performed (in order):\n  • .sietch/rig.config exists AND REPO_OWNER/REPO_NAME are not the example placeholders\n  • .beads/ exists (bd is the canonical task tracker per CLAUDE.md)\n  • gh CLI authenticated for REPO_SLUG (gh auth status -h github.com)\n  • .pre-commit-config.yaml exists at repo root\n  • pre-commit hooks actually installed (.git/hooks/pre-commit has the pre-commit-installed marker)\n  • .github/workflows/ has at least one .yml/.yaml file\n  • The first workflow runs on pull_request events (grep 'on:' block)\n  • A test directory exists (tests/ or test/ at repo root)\n  • At least one test file exists in that directory (test_*.py / *_test.py / *.test.{js,ts} / *_test.go)\n  • WORKTREE_BASE is writable (try mkdir -p; rmdir)\n- Verbose mode '-v' shows what each check tested and which path it inspected","status":"open","priority":2,"issue_type":"feature","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:02:43Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:02:43Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-jdb","title":"cut empty-cycle token cost via lib/eligibility.sh + exponential backoff","description":"With 'st loop' running, the dev-agent / reviewer-orchestrator / dispatcher loops wake on every tick and burn tokens loading their 30+ kB system prompts just to discover there's no work to do, then exit. On a quiet repo this is the dominant cost — most ticks are empty.\n\nFix: do the eligibility check in pure shell BEFORE invoking 'claude --append-system-prompt …'. Only spin up the agent when there's at least one candidate. Stack exponential backoff on top so cycle frequency itself decays when the queue's been quiet.","design":"Files added/changed:\n  - lib/eligibility.sh (new) — sourced by wrappers and invokable by agents via Bash\n  - templates/rig.config.example — add EMPTY_CYCLE_BACKOFF_BASE_SECONDS, EMPTY_CYCLE_BACKOFF_CAP_SECONDS, eligibility cache dir\n  - wrappers/run-developer.sh — preflight + early exit on empty\n  - wrappers/run-reviewer.sh — preflight + early exit on empty\n  - wrappers/run-loop.sh — backoff state per pane (~/.cache/sietch/\u003crig\u003e/\u003cmode\u003e.empty_cycles)\n  - templates/developer.md — replace ad-hoc 'gh issue list' with 'bash $SIETCH_HOME/lib/eligibility.sh dev'\n  - templates/reviewer-orchestrator.md — same swap\n\nCache shape (last-seen state, used to detect 'nothing changed since last empty cycle'):\n  ~/.cache/sietch/\u003crepo_slug_safe\u003e/\u003cmode\u003e.last-seen   # one-line digest, e.g. sha256 of sorted candidate IDs\n\nEligibility function contract (stdout = newline-delimited candidate IDs; exit 0 if any, exit 1 if none):\n  count=$(eligibility_dev_count)\n  if [ -z \"$count\" ]; then exit 1; fi\n\nOut of scope (file separately):\n  - Push-based triggering via GitHub webhooks (replace polling entirely) — bigger architectural shift\n  - Sharing the cache across multiple panes/loops (race conditions; the per-pane local cache is enough for v1)","acceptance_criteria":"- New lib/eligibility.sh with one named function per agent mode:\n  • eligibility_dev_count       — open severity:high|medium issues, no assignee, no in-flight dev-agent PR\n  • eligibility_review_pending  — dev-agent PRs not yet reviewed at current headRefOid\n  • eligibility_followup_pending — PRs with reviewer comment newer than dev-agent's last response\n  • eligibility_conflict_pending — PRs with mergeStateStatus DIRTY on ${BRANCH_PREFIX}/* branches\n- Each function reads rig.config (BRANCH_PREFIX, severity labels, etc.) and prints either an empty result or a list of candidate IDs/numbers + an exit code (0 = work, 1 = nothing)\n- run-developer.sh / run-reviewer.sh / run-loop.sh dispatchers source the helper and short-circuit BEFORE invoking 'claude' when eligibility is empty\n- Confirmed: empty-cycle invocation consumes zero LLM tokens (verified by running 'st dev' on a quiet repo and checking that no 'claude' subprocess fires)\n- Each agent template ALSO calls the same function as its first step, so the in-prompt re-check stays in sync — drift impossible\n- Exponential backoff on consecutive empty cycles: 60s → 120s → 240s → 480s, cap at ${EMPTY_CYCLE_BACKOFF_CAP_SECONDS:-300}, reset to base on first non-empty cycle. Implemented in run-loop.sh per-pane state file.","status":"closed","priority":2,"issue_type":"feature","assignee":"Anil Choudhary","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T01:23:04Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T01:33:18Z","started_at":"2026-05-07T01:25:21Z","closed_at":"2026-05-07T01:33:18Z","close_reason":"Shipped at sha 9778e90: lib/eligibility.sh with dev/review predicates, wrapper preflight in run-developer.sh + run-reviewer.sh (exit 2 on no-work), exponential backoff in loop_dev_mode1 + loop_reviewer (60→120→240→300, capped via EMPTY_CYCLE_BACKOFF_CAP_SECONDS=300), Step 0 fast-gate in developer.md + reviewer-orchestrator.md. Smoke-tested on quiet ai-cfo-eval-harness rig — both wrappers exit 2 without spawning claude.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-ajn","title":"add fixture-based tests for render-prompt, triage-conflict, and eligibility predicates","description":"The framework has zero tests today. The deterministic pieces — render-prompt.sh, triage-conflict.sh, lib/eligibility.sh — have enough conditional logic (regex matches, jq filter shapes, sed-vs-envsubst output, rule precedence in triage) that silent regressions are plausible.\n\nConcrete ways this bites today:\n  - render-prompt.sh has the REVIEWER_AGENT_VERDICT_REGEX drift (filed separately) — would have been caught by a fixture\n  - Triage rule order matters (test \u003e ci/secrets \u003e core \u003e line-limit) and is verified by reading code, not by a test\n  - eligibility jq filter for \"no review at headRefOid\" has subtle index/null handling that nothing exercises\n\nValidation today is \"the loop seemed to work in production.\" Sufficient for a single rig; will not scale to multi-rig or unattended use.","design":"Single dependency: bats-core (https://github.com/bats-core/bats-core). Already brew/apt installable; no language runtime.\n\nLayout:\n  tests/\n    fixtures/\n      rig.config              # canonical rig.config exercising every key\n      template-all-keys.md    # template referencing every rig key\n      conflicts/              # crafted files for triage cases\n        test-file-conflict/\n        ci-conflict/\n        core-file-conflict/\n        symmetric-5x5/        # the 10-line edge case\n        tractable-3-lines/\n      gh-fixtures/            # canned gh JSON for eligibility jq tests\n        no-pending-review.json\n        review-on-stale-sha.json\n        review-on-current-sha.json\n    test_render_prompt.bats\n    test_triage_conflict.bats\n    test_eligibility.bats\n    helpers.bash              # mock_gh, mock_jq if needed\n\nFor triage: run the script against each fixture by overriding REPO/REPO_SLUG and stubbing `gh pr view` via a PATH-prepended mock script. Assert the emit-line content (result=, reason=).\n\nFor eligibility: pipe gh-fixtures JSON through the same jq filters the script uses. Test the jq logic, not the gh call.\n\nFor render-prompt: render template-all-keys.md once with envsubst available, once with PATH stripped of envsubst (forces sed fallback), diff the outputs.\n\nCI: GitHub Actions workflow `ci.yml` running `bats tests/` on push and PR. Fast (\u003c10s). One job, one OS to start (ubuntu-latest); add macos-latest later if drift appears.","acceptance_criteria":"- tests/ directory with bats-core suite covering render-prompt, triage-conflict, and eligibility\n- Fixtures for each triage rule (test files / ci-secrets / core / line-limit / tractable / no-conflict)\n- render-prompt fixture exercises every rig.config key in both envsubst and sed-fallback modes; outputs are byte-identical\n- eligibility tests run the jq filters against canned gh JSON; cover the \"review at current headRefOid\" and \"review at stale headRefOid\" cases\n- README documents `bats tests/` as the test command\n- CI workflow on GH Actions runs tests on push/PR; turn time \u003c30s\n- Test suite passes against current code (i.e. don't sneak fixes into this issue — file separately)","status":"open","priority":3,"issue_type":"feature","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:11:07Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:11:07Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-2lp","title":"render-prompt.sh: sed fallback drifts from envsubst; key list duplicated 3 places","description":"lib/render-prompt.sh maintains the rig.config key list in three independent places:\n  - line 35-41: export line\n  - line 43-49: ALLOWLIST string for envsubst\n  - line 55-76: sed -e block (one line per key)\n\nAdding a new rig.config variable requires touching all three. Nothing enforces parity. The two render paths (envsubst vs sed fallback) can silently diverge — produce different rendered prompts on different machines depending on whether `envsubst` is available.\n\nToday: REVIEWER_AGENT_VERDICT_REGEX is in rig.config.example but NOT in any of the three lists in render-prompt.sh. So templates referencing $REVIEWER_AGENT_VERDICT_REGEX render literally as \"${REVIEWER_AGENT_VERDICT_REGEX}\". That's a real (silent) drift today.","design":"Option A — drop the sed fallback. envsubst (gettext) is in coreutils on every realistic platform; macOS ships it via gettext brew tap. README already lists it as \"optional\". Make it required, delete 22 lines, eliminate the drift surface entirely.\n\nOption B — single source of truth. Define `RIG_KEYS=(REPO_OWNER REPO_NAME ...)`, then build the export line, the envsubst ALLOWLIST, and the sed -e args programmatically from that array. Five-ish lines of array-driven code replace 40 lines of triplicated config.\n\nOption C — keep both paths, add a fixture test that renders a known template through both and asserts byte-identical output. Catches drift but doesn't prevent it.\n\nRecommendation: B. Keeps the fallback for users on minimal systems but eliminates the drift mechanism. While doing it, fix the missing REVIEWER_AGENT_VERDICT_REGEX.","acceptance_criteria":"- rig.config keys are listed exactly once in render-prompt.sh\n- Both render paths (envsubst and the fallback, if kept) reference that single list\n- REVIEWER_AGENT_VERDICT_REGEX renders correctly through both paths (the existing latent bug)\n- A grep for hard-coded variable names like `${REPO_OWNER}` or `--e \"s|${REPO_OWNER}` in render-prompt.sh returns at most one occurrence\n- README updated if the gettext dependency goes from \"optional\" to required (option A path only)\n- A smoke test loads a template referencing every key and asserts the output is fully substituted (paired with sietch-tests)","status":"open","priority":3,"issue_type":"bug","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:10:46Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:10:46Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-s3g","title":"triage line counter double-counts conflict sides; clarify TRIAGE_LINE_LIMIT semantics","description":"wrappers/triage-conflict.sh:120-134 walks each conflicted file and counts every non-marker line between \u003c\u003c\u003c\u003c\u003c\u003c\u003c and \u003e\u003e\u003e\u003e\u003e\u003e\u003e. The ======= separator is treated as \"don't count, don't toggle\" — so lines from BOTH the \"ours\" side and the \"theirs\" side accumulate into CONFLICT_LINES.\n\nA symmetric 5-vs-5 conflict shows as 10 against TRIAGE_LINE_LIMIT=10 and gets rejected. templates/rig.config.example:44 just says `TRIAGE_LINE_LIMIT=10` with no semantics. Users tuning the threshold will reason in \"lines of net change\" or \"lines on the larger side\" and be surprised.\n\nEither the implementation or the docs is wrong. Pick one and align.","design":"Three options, ordered by cost:\n\n(a) Doc-only fix: rewrite the rig.config.example comment and the script header to spell out \"TRIAGE_LINE_LIMIT counts lines on BOTH sides of every conflict region, summed across all conflicted files.\" Cheapest. The number 10 stays — but means \"5+5\" in practice, which is conservative-by-default. Probably fine.\n\n(b) Toggle off at =======: count only the \"ours\" side. Simpler interpretation but discards \"theirs\" complexity, which is the side the agent has to reason about during rebase. Risky — undercounts merge complexity.\n\n(c) Track ours_lines and theirs_lines separately, threshold on max(ours, theirs) per conflict, summed across files. Most defensible semantically. Slightly more code.\n\nRecommendation: (a) for v1 (minutes of work, no behavior change). Revisit (c) if/when triage starts wrongly rejecting tractable conflicts in practice.","acceptance_criteria":"- rig.config.example documents what TRIAGE_LINE_LIMIT actually counts (concrete example: \"5 lines ours + 5 lines theirs = 10\")\n- triage-conflict.sh header comment matches the rig.config doc\n- If implementation changes (option b/c), default value is re-tuned so existing tractable PRs still pass triage\n- Triage emit line includes a reproducible breakdown (e.g. ours=N theirs=M files=K)\n- A fixture test exercises a 5+5 symmetric conflict against the chosen threshold (paired with sietch-tests)","status":"open","priority":3,"issue_type":"bug","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:10:32Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:10:32Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-byb","title":"st init: write a SIETCH marker-block into the rig's CLAUDE.md","description":"Sietch currently has no programmatic way to communicate framework-specific guidance to a Claude session running inside a consumer rig. A human Claude session has no idea that the dev-agent owns 'BRANCH_PREFIX/*' branches, that locks live under LOCK_DIR, or which 'st *' commands are available — it has to be told manually each time, or the user has to hand-write the same boilerplate into every rig's CLAUDE.md.\n\nMirror bd's pattern: a marker-bracketed block in CLAUDE.md, written by 'st init' and refreshed by a future 'st sync'. Closes the onboarding loop — 'st init' writes rig.config AND the CLAUDE.md block, so a new rig is genuinely one-step.","design":"New template: templates/claude-md-block.md — parameterized, rendered through the existing lib/render-prompt.sh pipeline.\n\nbin/st cmd_init extended: after copying rig.config from the example, render claude-md-block.md and either:\n  - append it to existing CLAUDE.md (between markers, preserving the rest), OR\n  - write a new CLAUDE.md containing only the block + a stub heading\n\nHash computed by piping rendered block (without marker lines) into 'shasum -a 256 | cut -c1-8' and substituted into the BEGIN marker.\n\nIdempotency: a small awk/sed pass detects the BEGIN/END pair. If present, skip; if absent, append. (Refresh-when-hash-stale is the 'st sync' job.)\n\nOut of scope (file separately if/when needed):\n  - 'st sync' to refresh the block on framework upgrade\n  - Auto-detection of stale blocks across rigs\n  - Per-rig customization of block content (beyond rig.config substitutions)","acceptance_criteria":"- 'st init' writes a SIETCH marker-block into the rig's CLAUDE.md\n- Creates CLAUDE.md if it doesn't exist; preserves all user content outside the block if it does\n- Idempotent: re-running 'st init' does not duplicate the block; if a block exists, leaves it alone (refresh is the future 'st sync' job)\n- Marker format mirrors bd: \u003c!-- BEGIN SIETCH INTEGRATION v:1 hash:\u003csha8\u003e --\u003e … \u003c!-- END SIETCH INTEGRATION --\u003e\n- Block content includes:\n  • one-paragraph mental model (this rig is a sietch consumer; framework lives at ~/code/sietch)\n  • command reference (st dev / st review / st loop start / st help)\n  • coordinate-with-dev-agent rules (don't push to BRANCH_PREFIX/*, don't tamper with LOCK_DIR, dev-agent uses filesystem locks for atomic claim)\n  • pointer to .sietch/rig.config for tunables\n- Block is parameterized — rig.config variables (BRANCH_PREFIX, LOCK_DIR, etc.) substituted via the existing render-prompt.sh\n- Block hash is sha256 of rendered content truncated to 8 chars (used by future 'st sync' for drift detection)\n- AGENTS.md is NOT touched — bd's choice to write to both files is a redundancy we don't repeat","status":"open","priority":3,"issue_type":"feature","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T01:05:20Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T01:05:20Z","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"sietch-yer","title":"add 'st permissions install' to bootstrap user-level Claude grants","description":"When a new user installs sietch, Claude Code re-prompts them for every Bash/MCP grant the agents need (gh, git, bd, python, pre-commit, tmux, …). A self-contained 'st permissions install' command should remove that friction so onboarding a rig is genuinely one-step.\n\nMigrated from ai-cfo-eval-harness-a4fi (filed there before this repo had its own bd workspace).","design":"Subcommand 'permissions' added to bin/st with dispatch to lib/permissions.sh.\n\nGrant list lives in lib/permissions.json — single source of truth, readable by users.\nScope = grants the four sietch agents actually invoke; NOT personal QoL:\n  gh *, git *, bd *, st *, python *, python3 *, pytest *,\n  pre-commit run *, ruff *, tmux *, pkill *, kill *, bash, jq *, mkdir *, chmod *\n\nImplementation:\n  - lib/permissions.sh reads lib/permissions.json (jq parses)\n  - Reads existing ~/.claude/settings.json (creates from template if missing)\n  - Computes set difference (new - existing) using jq\n  - On install: cp settings.json settings.json.sietch.bak; jq merge writes new file\n  - On --dry-run: print the diff list (one entry per line)\n  - On list: cat lib/permissions.json | jq '.allow'\n  - Atomic write: jq → temp file → mv into place\n\nOut of scope (file separately if needed):\n  - Project-specific grants (e.g. .venv/, PYTHONPATH=src) — those still belong in repo's .claude/settings.local.json\n  - 'st permissions uninstall' (revert to backup) — nice-to-have, not in v1\n  - Auto-running install during 'st init' — keep explicit; user opts in","acceptance_criteria":"- 'st permissions install' merges sietch-required grants into ~/.claude/settings.json\n- Idempotent: re-running adds nothing if all entries already present (compare by exact string match)\n- '--dry-run' prints the diff (entries that would be added) and exits 0 without writing\n- 'st permissions list' prints the grant list as JSON (no I/O on settings.json)\n- A backup is written to ~/.claude/settings.json.sietch.bak before any modification\n- New ~/.claude/ is handled: creates settings.json with {permissions: {allow: [...]}} if missing\n- JSON is validated (jq -e .) before and after; refuses to write if pre-state is invalid\n- Clear error if jq is missing on PATH ('install jq via brew install jq')\n- Top-level non-permission keys (e.g. effortLevel, skipAutoPermissionPrompt) are preserved","status":"open","priority":3,"issue_type":"feature","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T00:56:15Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T00:56:15Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-kz0","title":"reconcile AGENTS.md cp/rm/mv -i alias warning with what wrappers actually do","description":"AGENTS.md:14-31 prominently warns that aliased cp/mv/rm with -i can hang headless agents on confirmation prompts, and instructs use of `-f` flags throughout. But the warning is aimed at the LLM agents Claude is running INSIDE the wrappers — the wrappers themselves invoke `cp`, `rm`, `mv` directly without `command cp`, `\\rm`, or alias defenses.\n\nIn practice: bash does NOT expand aliases in non-interactive scripts (the wrappers run without `-i`), so this is mostly defensive. But the asymmetry is confusing — either the warning matters and the wrappers should harden, or it doesn't and the warning is overstated. Either way, document or fix.","design":"Pick one:\n\nA) Add a one-line defense at the top of every wrappers/*.sh and lib/*.sh:\n   `\\unalias -a \u003e/dev/null 2\u003e\u00261 || true`\n   Costs nothing, removes any chance of an interactive shell startup file biting us.\n\nB) Edit AGENTS.md to clarify that the warning applies to LLM-issued shell calls (which Claude's runtime handles via approval prompts anyway), not to the wrappers, and shorten the section.\n\nC) Both: do A (cheap belt-and-braces) and tighten AGENTS.md to match.\n\nRecommendation: C. Two-minute change, eliminates the asymmetry entirely.","acceptance_criteria":"- Every wrappers/*.sh and lib/*.sh either has the unalias defense at top, OR a comment justifying its absence\n- AGENTS.md alias section either confirms the wrappers are protected, OR is explicitly scoped to LLM-issued shell calls\n- No behavior change in the happy path — just tightening","status":"open","priority":4,"issue_type":"bug","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T03:11:19Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T03:11:19Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"sietch-k1s","title":"refresh stale CLAUDE.md marker-block when framework upgrades (st sync vs implicit)","description":"Once sietch-byb ships and 'st init' writes a SIETCH marker-block with a content hash, rigs will go stale silently when ~/code/sietch is git-pulled and the template changes. Need a way to detect and refresh stale blocks.\n\nDESIGN QUESTION (deliberately deferred until the first real stale-block moment): explicit 'st sync' command vs implicit refresh on every 'st *' invocation (the pattern bd uses). bd's implicit approach is ergonomically nicer — no command to remember, never out of date — but adds a cost (and a moving target) to every CLI call. Decide based on (a) how often templates actually change, (b) whether there's ever a multi-rig propagation pain.\n\nFiled P4: don't build until there's evidence of need (a template change worth propagating across multiple rigs).","design":"Two paths to evaluate when this is built:\n\nA) Explicit 'st sync':\n   - bin/st adds 'sync' subcommand\n   - lib/sync.sh: render template → hash → compare to rig's marker → if stale, replace block\n   - User must remember to run after 'cd ~/code/sietch \u0026\u0026 git pull'\n\nB) Implicit refresh:\n   - Wrap every 'st *' subcommand entry in a stale-block check\n   - On stale: print one-line notice + refresh + continue\n   - Cache last-seen-template-hash in ~/.cache/sietch/ to keep the check cheap\n\nRecommendation when building: start with B (matches bd's pattern; ergonomically cleaner) but only if multi-rig propagation is real. If single-rig is permanent, neither is worth building — just re-run 'st init --force-block-refresh' manually after big upgrades.\n\nOut of scope:\n  - Refreshing rig.config (different problem; user values must not be clobbered)\n  - Cross-rig discovery (sietch doesn't track which rigs exist)","acceptance_criteria":"- Detect stale marker-block: compare hash in CLAUDE.md's BEGIN marker against fresh hash of rendered current template\n- Refresh stale block in place, preserving all CLAUDE.md content outside the markers\n- Picked design (explicit 'st sync' or implicit on every 'st *') is informed by actual usage, not guessed\n- If implicit: refresh path adds no measurable latency to common commands (cache the hash check)\n- If explicit: 'st sync' is idempotent (no-op when already current) and supports '--dry-run' to print the diff","status":"open","priority":4,"issue_type":"feature","owner":"anil_choudhary@mckinsey.com","created_at":"2026-05-07T01:09:26Z","created_by":"Anil Choudhary","updated_at":"2026-05-07T01:09:26Z","dependencies":[{"issue_id":"sietch-k1s","depends_on_id":"sietch-byb","type":"blocks","created_at":"2026-05-07T09:09:34Z","created_by":"Anil Choudhary","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
